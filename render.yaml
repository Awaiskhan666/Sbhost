services:
  - type: web
    name: sbhost1
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:application
    envVars:
      - key: SESSION_SECRET
        generateValue: true
